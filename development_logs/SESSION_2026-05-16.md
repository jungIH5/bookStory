# 작업 세션 — 2026-05-16

## 개요

bookStory 웹앱을 React Native + Expo 모바일 앱으로 전환하는 작업의 연속 세션.  
이전 커밋(`앱으로 전면 개편`)에서 구축한 기반 위에 **보안 강화 + 품질 버그 수정**을 집중 처리.

---

## 1. 보안 강화

### 1-1. JWE (JSON Web Encryption) 토큰 도입 — `backend/index.js`

- **변경 전**: JWT 미사용 (userId를 쿼리파라미터·바디로 직접 전달)
- **변경 후**: `jose` v4 라이브러리로 JWE 발급/검증
  - 알고리즘: `dir` + `A256GCM` (대칭키 암호화)
  - `JWT_SECRET` 환경 변수로 키 관리
  - `requireAuth` 미들웨어: `Authorization: Bearer <token>` 헤더에서 userId 추출
  - CommonJS 환경에서 ES Module인 jose를 쓰기 위해 dynamic import 사용

```
POST /api/users      → { user, token } 반환
POST /api/auth/token → 토큰 갱신
```

### 1-2. CORS 화이트리스트 — `backend/index.js`, `agent/main.py`

- **변경 전**: `cors({ origin: "*" })`
- **변경 후**: `ALLOWED_ORIGINS` 환경 변수로 허용 출처 관리
  - 모바일 네이티브 요청(origin 없음)은 항상 허용
  - Node: `cors({ origin: (origin, cb) => { ... } })`
  - Python: `CORSMiddleware(allow_origins=allowed_origins)`

### 1-3. 브루트포스 방어 — `backend/index.js`

`express-rate-limit` 적용:

| 리미터 | 경로 | 제한 |
|--------|------|------|
| `globalLimiter` | 전체 | 200 req / 15분 |
| `registerLimiter` | `POST /api/users` | 10 req / 1시간 |
| `aiLimiter` | `POST /api/books/analyze` | 30 req / 1시간 |

### 1-4. Python 백엔드 보안 — `agent/main.py`, `agent/auth.py`(신규)

- `slowapi` rate limiter (200/15min) 추가
- `auth.py`: `python-jose`로 JWE 검증하는 `get_current_user_id()` FastAPI Depends 구현
- `requirements.txt`: `python-jose[cryptography]>=3.3.0`, `slowapi>=0.1.9` 추가

### 1-5. 입력 유효성 검사 — `backend/index.js`

```js
isValidName(v)  // 한글·영문, 1-50자
isValidAge(v)   // 1-120 정수
isValidText(v)  // 5000자 이하
```

POST /api/users, /api/community/posts 등 사용자 입력 엔드포인트에 적용.

---

## 2. 모바일 스토어·서비스 레이어 재작성

### 2-1. `mobile/store/useUserStore.ts`

- `token: string | null` 필드 추가
- `setToken(token)`: `expo-secure-store`에 암호화 저장 (민감 데이터)
- `setUser(null)`: 로그아웃 시 SecureStore 토큰도 함께 삭제
- `loadUser()`: AsyncStorage(프로필) + SecureStore(토큰) 병렬 복원

### 2-2. `mobile/services/api.ts`

- 모든 요청에 `Authorization: Bearer <token>` 자동 주입
- 401 응답 → 자동 로그아웃
- 429 응답 → `rate_limited` 에러
- `usersApi.create`: `{ user, token }` 응답 처리 + 토큰 저장
- `usersApi.refreshToken`: 신규 토큰 갱신 메서드
- `recordingsApi.upload`: 파일 확장자로 MIME 타입 자동 감지
- `tendencyApi.getRecommendations`: `BookRecommendation[]` 올바른 타입 반환
- `clubsApi.getJoined()`: userId 파라미터 제거 (토큰으로 서버에서 판단)

### 2-3. 스토어 dead code 제거

| 파일 | 제거된 항목 |
|------|------------|
| `useBooksStore.ts` | searchResults, selectedBook, analysis, isSearching, isAnalyzing |
| `useClubsStore.ts` | selectedClub, isLoading, setSelectedClub, setIsLoading |
| `useCommunityStore.ts` | isLoading, setIsLoading |

---

## 3. 모바일 화면 품질 수정

### 3-1. 앱 초기화 — `mobile/app/_layout.tsx`

- `loadUser()` 완료 후 SplashScreen 숨김 (hydration flash 방지)
- 로딩 중 `null` 반환으로 인증 상태 불안정 렌더링 제거

### 3-2. 오프라인 감지 — `index.tsx`, `clubs.tsx`, `community.tsx`, `search.tsx`, `recording.tsx`

모든 탭 화면에 NetInfo 기반 오프라인 처리 통일:

- `MotiView` 슬라이드인 배너 (`#374151` 다크 배경)
- 네트워크 복구 시 자동 재fetch
- `search.tsx`: 오프라인 상태에서 검색 시도 시 Alert
- `recording.tsx`: 오프라인 배너 + 분석 제한 메시지

### 3-3. NetInfo stale closure 버그 수정 — `clubs.tsx`, `community.tsx`

- **문제**: NetInfo 콜백에서 `fetchError` state가 초기값(false)으로 고정됨
- **해결**: `useRef`로 `fetchErrorRef`를 동기 추적, 콜백에서 ref 참조

```ts
// Before — 항상 false (stale closure)
if (!offline && fetchError) fetchData();

// After — 최신값 참조
const fetchErrorRef = useRef(false);
if (!offline && fetchErrorRef.current) fetchData();
```

### 3-4. 키보드 dismiss — `profile.tsx`, `clubs.tsx`, `community.tsx`

폼이 있는 화면/모달에 `TouchableWithoutFeedback + Keyboard.dismiss` 적용:
- `RegisterForm` (profile.tsx)
- `CreateClubModal` (clubs.tsx)
- `WritePostModal` (community.tsx)
- `ScrollView`에 `keyboardShouldPersistTaps="handled"` 추가

### 3-5. 이미지 fallback — `search.tsx`

`book.image`가 빈 문자열이거나 없을 때 `BookOpen` 아이콘 placeholder로 대체  
(검색 결과 카드 + 분석 모달 양쪽 모두 처리)

### 3-6. 타입 수정 — `recommendations.tsx`

- `as any` 제거
- `Book & { reason?: string }` → `BookRecommendation` 타입 직접 사용

---

## 4. 환경 변수

### `backend/.env` 추가 항목

```
JWT_SECRET=your_jwt_secret_min_32_chars_here
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:19006
```

### `agent/.env` 신규 생성

```
ANTHROPIC_API_KEY=your_anthropic_api_key
JWT_SECRET=your_jwt_secret_min_32_chars_here  # backend와 동일 값
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:19006
PORT=8000
```

> **중요**: `JWT_SECRET`은 Node.js(jose)와 Python(python-jose) 양 백엔드가 공유.  
> 운영 환경에서는 `openssl rand -base64 32` 등으로 생성한 랜덤 값 사용.

---

## 5. 변경된 파일 목록

```
backend/index.js              보안 전면 재작성 (JWE, CORS, rate limit, 입력 검증)
backend/package.json          jose, express-rate-limit, helmet 의존성 추가
backend/.env                  JWT_SECRET, ALLOWED_ORIGINS 추가
agent/main.py                 CORS 화이트리스트, slowapi rate limiter
agent/auth.py                 신규 — JWE 검증 FastAPI Depends
agent/requirements.txt        python-jose, slowapi 추가
agent/.env                    신규 생성
mobile/app/_layout.tsx        hydration 수정 (loadUser 후 SplashScreen 숨김)
mobile/app/(tabs)/index.tsx   홈 아바타, 오프라인 배너, 에러 배너
mobile/app/(tabs)/clubs.tsx   오프라인 처리, stale closure 수정, 키보드 dismiss
mobile/app/(tabs)/community.tsx  오프라인 처리, stale closure 수정, 키보드 dismiss
mobile/app/(tabs)/profile.tsx 키보드 dismiss
mobile/app/(tabs)/search.tsx  오프라인 처리, 이미지 fallback
mobile/app/(tabs)/recording.tsx  오프라인 처리 추가
mobile/app/recommendations.tsx   as any 제거, BookRecommendation 타입
mobile/services/api.ts        JWT 주입, 401/429 처리, 타입 정리
mobile/store/useUserStore.ts  token + SecureStore 통합
mobile/store/useBooksStore.ts dead code 제거
mobile/store/useClubsStore.ts dead code 제거
mobile/store/useCommunityStore.ts dead code 제거
mobile/app.json               expo-maps 플러그인 제거
```

---

## 6. 남은 작업

- [ ] `agent/routers/*.py` — 보호 엔드포인트에 `get_current_user_id` Depends 적용
- [ ] EAS Build 설정 및 TestFlight/Google Play 내부 테스트 배포
- [ ] `JWT_SECRET` 운영 환경 키 발급 및 인프라 환경 변수 설정
